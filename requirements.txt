# Core dependencies
torch>=2.0.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.8.0
trl>=0.8.0

# Unsloth - for fast fine-tuning
unsloth[cu121]>=2024.5.0

# ONNX export for transformers.js
optimum[exporters]>=1.16.0
onnx>=1.15.0
onnxruntime>=1.16.0

# Utilities
huggingface-hub>=0.20.0
tqdm>=4.66.0
numpy>=1.24.0
scipy>=1.10.0

# Jupyter notebook support (optional)
jupyter>=1.0.0
ipykernel>=6.25.0

# Optional: for Axolotl alternative
# axolotl[flash-attn,deepspeed]>=0.4.0
